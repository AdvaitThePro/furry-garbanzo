import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chargecars1',
  appName: 'chargecars1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
